using System.Collections;
using System.Collections.Generic;
using UnityEngine;
// using UnityEngine.XR.ARFoundation;

public class AddARPlaneManager : MonoBehaviour
{
    // // Start is called before the first frame update
    // void Start()
    // {
    //     // Check if ARPlaneManager is already added, if not, add it
    //     if (GetComponent<ARPlaneManager>() == null)
    //     {
    //         gameObject.AddComponent<ARPlaneManager>();
    //     }

    //     // Optionally enable plane detection
    //     ARPlaneManager planeManager = GetComponent<ARPlaneManager>();
    //     planeManager.enabled = true;
    // }

    // // Update is called once per frame
    // void Update()
    // {

    // }
}
