using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RandomParticleSprite : MonoBehaviour
 {
//     public Sprite[] sprites; // Drag and drop the sprites from the PNG file into this array in the Inspector

//     private ParticleSystemRenderer particleRenderer;

//     void Start()
//     {
//         particleRenderer = GetComponent<ParticleSystemRenderer>();

//         if (sprites.Length > 0)
//         {
//             particleRenderer.sprite = sprites[Random.Range(0, sprites.Length)];
//         }
//     }
}
